# Add custom ENV values here to export
export GOPATH="$HOME/go"
export M2_HOME="$HOME/apache-maven-3.8.8"

# GitHub Copilot CLI Configuration
export COPILOT_CLI_EDITOR="$EDITOR"

# Add ~/.bin to PATH
export PATH="$HOME/.bin:$PATH" 

