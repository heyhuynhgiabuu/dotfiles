    -- Load options
    require("options")

    -- Load keymaps
    require("keymaps")

    -- Load plugins with lazy.nvim
    require("lazy-setup")

    -- Load LSP
    require("lsp")

    -- Load floating term
    require("floating-term")

    -- Load help floating
    require("help-floating")

    -- Load snipets
    require("snipets")

    -- Load vim helpers
    require("vim-helpers")
